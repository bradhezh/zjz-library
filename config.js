const USERS_ROUTE = '/api/users'
const LOGIN_ROUTE = '/api/login'
const ITEMS_ROUTE = '/api/items'
const BY_ID = '/:id'
const BY_NAME = '/name/:name'
const BY_USERID = '/userid/:userid'
const BY_USERNAME = '/username/:username'
const BY_USER = '/user'
const BY_ADMIN = '/admin'
const BY_AVAILABLE = '/available'
const BY_ID_ADMIN = '/:id/admin'

const ADMIN_ROLE = 'admin'
const USER_ROLE = 'user'
const OWNER_ROLE = 'owner'

module.exports = {
  USERS_ROUTE,
  LOGIN_ROUTE,
  ITEMS_ROUTE,
  BY_ID,
  BY_NAME,
  BY_USERID,
  BY_USERNAME,
  BY_USER,
  BY_ADMIN,
  BY_AVAILABLE,
  BY_ID_ADMIN,
  ADMIN_ROLE,
  USER_ROLE,
  OWNER_ROLE,
}
